/* suvvy-widgeticons.js — заглушка вместо tilda-menu-widgeticons-1.0 (7,5 КБ).
 *
 * Оригинал вешает клики на иконки-виджеты шапки: корзину, поиск, личный
 * кабинет, избранное (.t-menuwidgeticons__link_*). На suvvy.ai таких элементов
 * нет ни на одной странице (проверено грепом по всем блокам) — файл был
 * мёртвым грузом. Снять совсем нельзя: инлайн-код шапки на 48 страницах зовёт
 * t_onFuncLoad('t_menuWidgets__init', …), и без имени поллинг через 15 секунд
 * бросил бы ошибку. Поэтому имя остаётся, работа — нет.
 */
window.t_menuWidgets__init = function () {};
